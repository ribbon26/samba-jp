<samba:parameter name="smb encrypt"
                 context="S"
				 type="enum"
                 basic="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>

    <para>これは、Samba3.2以降で導入された新機能である。この機能は、SMB/CIFS
        プロトコルを拡張し、UNIX拡張機能の一部としてネゴシエートされる。
	SMB暗号化は、SMBプロトコルストリーム中のすべてのリクエスト/レスポンス
	に対し、暗号化と署名を行うためにGSSAPI(Windows上のSSPI)を使う。
	SMB暗号化を有効にすると、sshの保護されたセッションと同じように、
        暗号化のネゴシエートとキーの署名のためにSMB/CIFS認証を使って
        セキュアなSMB/CIFS通信を提供する。
        現在、これは、Samba 3.2のsmbclientのみでサポートされており、
        そのうちLinuxのCIFSFSとMacOS/Xクライアントで使えるようになるだろう。
        Windowsクライアントはこの機能をサポートしない。
    </para>

    <para>
        この機能はサーバーが通信相手のクライアントに対してSMB暗号化を
	提供、あるいは要求するかどうかを制御する。
	利用できる値は、<emphasis>auto</emphasis>, <emphasis>mandatory</emphasis> 
	と<emphasis>disabled</emphasis>である。これは、共有毎に設定できるが、
	クライアントはすべてのセッション全体を暗号化するかもしれないし、
	特定の共有へのトラフィックだけ暗号化を利用しないかもしれない。
	もしもこれが mandatory に設定された場合、その共有へ接続が行われると、
	その共有へのすべてのトラフィックは暗号化されなければ 
	<emphasis>ならない</emphasis>。
	サーバはそのような共有に対する暗号化されていない要求に対し、
	"access denied" を返す。暗号化されたトラフィックを選択すると、
	すべてのデータへの暗号化と署名のオーバヘッドと同じように、より小さな
	パケットサイズを使わねばならない(UNIX風の大きなサイズでの
	読み出し/書き込みが許可されない)のでスループットが減少する。
    </para>

    <para>もしもSMB暗号化を選ぶと、Windows風のSMB署名(<smbconfoption name="server signing"/>
	を参照)はもはや必要でなくなり、GSSAPIフラグがデータの暗号化と署名とシール
	のために使われる。</para>

    <para>autoに設定すると、SMB暗号化が提供されるが、強制はされない。
	mandatoryに設定すると、SMB暗号化が要求され、もしもdisableに設定すると、
	SMB暗号化は利用出来ない。</para>
</description>

<value type="default">auto</value>
</samba:parameter>
